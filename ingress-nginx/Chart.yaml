apiVersion: v2
name: ingress-nginx-forge
description: Umbrella chart wrapping kubernetes/ingress-nginx for the mgmt-forge cluster
type: application
version: 0.1.0
appVersion: "1.11.3"

# Upstream chart from https://kubernetes.github.io/ingress-nginx
# Version pin here — bump to upgrade ingress-nginx cluster-wide.
dependencies:
  - name: ingress-nginx
    version: 4.11.3
    repository: https://kubernetes.github.io/ingress-nginx
