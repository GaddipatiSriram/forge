apiVersion: v2
name: external-secrets-forge
description: Umbrella chart wrapping external-secrets-io/external-secrets for mgmt-forge
type: application
version: 0.1.0
appVersion: "0.10.4"

# Upstream chart from https://charts.external-secrets.io
# ESO controller + webhook + cert-controller are installed as one release.
dependencies:
  - name: external-secrets
    version: "0.10.4"
    repository: https://charts.external-secrets.io
